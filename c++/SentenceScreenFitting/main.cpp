#include <iostream>
#include <vector>
#include <string>
#include <unordered_map>
using namespace std;

int wordsTyping(vector<string>& sentence, int rows, int cols){
	if(!rows || !cols) return 0;
	int sz = sentence.size();
	vector<string> 
	return 0;
}

int main(int argc, char const *argv[])
{
	int rows = 4;
	int cols = 5;
	vector<string> sentence = {"I","had","apple","pie"};
	cout << wordsTyping(sentence, row, cols) << endl;
	return 0;
}